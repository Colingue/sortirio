# Project description

Sortirio: a mobile app (iOS + Android, Expo / React Native) for making **friends**
in a new city. Not a dating app.

# Expo documentation

Expo has changed -> Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Architecture decisions (ADRs)

Structural decisions are documented in the `docs/adr/` directory:

**Check the ADRs first.** Before any planification, read the relevant ADRs. If a
change makes a statement in an ADR obsolete, update that ADR as part of the same work.
If a change calls for a new structural decision, ask for confirmation before creating a
new ADR (copy `docs/adr/000-template.md` under the next available number, then add it to the
list above).
